---
title: 本地化
description: 保持 UI 文案在所有支持语言中同步。
icon: Languages
---

桌面应用本地化资源位于：

```text
CodexSwitch/Assets/i18n/*.json
```

运行时通过 `I18nService` 和 `TrExtension` markup extension 加载本地化 catalog。

修改 UI 文案时，更新所有支持的 locale 文件，并运行聚焦的 i18n 测试。
