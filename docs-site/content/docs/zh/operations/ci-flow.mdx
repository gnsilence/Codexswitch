---
title: CI 流程
description: 仓库 CI 在发布前验证哪些内容。
icon: GitBranch
---

`ci` workflow 会验证 changelog 状态，恢复包，构建测试项目，运行 xUnit 测试，并在平台 runner 上发布 Native AOT 产物。

## 标准检查

```powershell
dotnet restore CodexSwitch.Tests/CodexSwitch.Tests.csproj
dotnet build CodexSwitch.Tests/CodexSwitch.Tests.csproj -c Release --no-restore
dotnet test CodexSwitch.Tests/CodexSwitch.Tests.csproj -c Release --no-build --no-restore
```

## Release 触发

当 workflow 运行在 `vX.Y.Z` tag 上时，会检查 tag 是否匹配 `Directory.Build.props`，创建 GitHub Release，并上传生成的产物。
