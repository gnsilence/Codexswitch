---
title: Localization
description: Keep UI text synchronized across supported locale files.
icon: Languages
---

Desktop localization uses JSON resources under:

```text
CodexSwitch/Assets/i18n/*.json
```

The runtime catalog is loaded through `I18nService` and the `TrExtension` markup extension.

When changing UI text, update all supported locale files and run focused i18n tests.
