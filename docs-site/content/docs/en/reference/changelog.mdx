---
title: Changelog
description: Release history source and where to read the full repository changelog.
icon: ListRestart
---

The canonical changelog remains at the repository root:

```text
CHANGELOG.md
```

Use it for feature history around sessions, WebSocket proxying, Claude Code, OAuth, usage dashboard, update checks, and packaging.

Release validation expects the first release heading to match the version in `Directory.Build.props`.
